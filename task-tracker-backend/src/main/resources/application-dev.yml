spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  flyway:
    locations: classpath:db/migration
jwt:
  secret-key: aec497118a6e0a13cfcc1f03302c4147
  # 1 hour in milliseconds
  expiration-time: 3600000